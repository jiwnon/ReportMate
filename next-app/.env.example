# =============================================================================
# ReportMate - 환경변수 예시 (Supabase, NextAuth, wrangler)
# =============================================================================
# 복사 후 .env.local 로 저장. .env.local 은 Git 제외 (.gitignore).
# Cloudflare 배포 시 Dashboard > Environment variables 에 설정.
# =============================================================================

# Supabase (필수) - Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# NextAuth (Google 로그인) - 로컬에서 로그인 버튼 동작하려면 아래 4개 필수
NEXTAUTH_SECRET=your-random-secret-at-least-32-chars
NEXTAUTH_URL=http://localhost:3000
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# [로그인 버튼 안 될 때] 1) .env.local 에 위 4개 값 확인 2) 수정 후 npm run dev 재시작
# 3) Google OAuth 동의 화면: 테스트 앱이면 "테스트 사용자"에 이메일 추가
# 4) 승인된 리디렉션 URI: 로컬 http://localhost:3000/api/auth/callback/google
#    배포 https://report-mate.org/api/auth/callback/google
# 5) 배포( report-mate.org )는 Cloudflare Variables/Secrets 에 설정

# 연결 풀링: Supabase 대시보드에서 "Connection pooling" (Transaction mode) URL 사용 권장
# SUPABASE_SERVICE_ROLE_KEY: 서버 전용, RLS 우회 등. 클라이언트 노출 금지
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
